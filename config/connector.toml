# The connector in front of the Anyone Protocol credentials issuer.
#
# THIS NODE IS REACHED AT A HIDDEN-SERVICE ADDRESS. It publishes no clearnet
# endpoint and no public port: the `anon` sidecar in compose.yml is the ingress,
# and `[node].http_endpoint` below is where buyers are told to find it. A
# hidden-service endpoint hides WHERE THIS NODE IS REACHABLE and nothing else --
# see the settlement block at the foot of this file, which is not covered by it.
#
# This node terminates its own routes and does not peer: see
# docs/adr/0002-payment-reference-is-the-paying-channel-identity.md for why that
# matters. Because nobody forwards to us, we always collect, so X-TOON-Payer is
# always present on a delivery and the minter's fail-closed path is an assertion
# rather than a routine outcome. Adding a peer is the change that forces that
# decision to be revisited.

client_edge_addr = "0.0.0.0:3000"
state_dir = "/app/state"

# The HTTP carriage must be exposed: a client selects its transport from the
# carriages this node advertises, and "neither" leaves it with none to choose
# (the client fails with TRANSPORT_REQUIRED before it sends anything).
# Exposing a carriage is not peering: no peer is configured, so nothing
# forwards to us and we always collect our own payments.
peer_expose = "http"

[signer]
key_file = "/app/data/signer.key"

[node]
addresses = ["g.anyone.credentials"]
# Required whenever a carriage is exposed, and deliberately without a default:
# a node cannot learn its own public name. That is ADR 0050's shape, and it is
# why this key exists at all.
#
# THE HOST BELOW IS A PLACEHOLDER, committed as one on purpose. The `anon`
# sidecar generates the real address into its HiddenServiceDir/hostname the
# first time it runs, and the OPERATOR copies it in -- the connector neither
# reads that file nor speaks the daemon's control protocol (connector ADR 0070
# decision 7). `make up` runs `scripts/hs-address.sh`, which plays the operator:
# it reads the address and renders this file into
# config/.rendered/connector.toml, WHICH IS WHAT COMPOSE MOUNTS. A placeholder
# ends in `.anyone` too, so this committed file still loads through the real
# parser.
#
# `.anyone`, NOT `.onion`. Anyone Protocol renamed the hidden-service TLD and
# the rename is total: the daemon this stack runs writes `.anyone` and refuses
# `.onion`, and the toon client rejects `.onion` by name because that is Tor and
# not `anon`. See anon-image/Dockerfile.
#
# NO NEW KEY (decision 6): a hidden-service URL is a legal value for the
# `http_endpoint` this section already had.
#
# AND NO `peer_allow_plaintext_endpoints`. The address IS the ed25519 public key
# the circuit is authenticated to, so `http://` at such a host authenticates
# itself -- more strongly than a CA-issued certificate for a DNS name does.
# ADR 0004's requirement is satisfied by a different mechanism rather than
# waived. `is_onion_endpoint` matches `.anyone` as well as `.onion` only since
# issue #1284; on an older connector image this config would need that opt-in,
# which is a loopback-and-test switch and must never be set here. That is why
# compose.yml pins the image it does.
http_endpoint = "http://placeholderplaceholderplaceholderplaceholderplaceholdera.anyone/ilp"

# NO `socks_proxy`, and that is a decision rather than an omission. That key
# selects a SOCKS5 proxy for OUTBOUND dials whose host is a hidden service
# (decision 3). This node has no `[[peers]]` and no `[[pay_channels]]`: it
# terminates its own routes and always collects, so it dials nothing over ILP
# and the key would select nothing. Everything it does dial -- the settlement
# RPC below, and `handler_url` above -- goes direct BY DESIGN (decision 4).
#
# Adding a peer is the change that forces this to be revisited, alongside the
# payment-reference decision in ADR 0002.

# --- Routes -----------------------------------------------------------------
#
# PRICE COUPLING: 10000000000000000 base units / 10^18 (ANYONE decimals) = 0.01
# ANYONE, which must equal the issuer's BUNDLE_PRICE exactly. The issuer
# verifies the amount inside the signed claim against its own BUNDLE_PRICE, so
# if these three drift -- this file, and BUNDLE_PRICE on both the claim-minter
# and the issuer in compose.yml -- every paid request returns 402 CLAIM_INVALID.
#
# Only the base-unit figure changes with the token; the decimal "0.01" that the
# minter signs and the issuer checks stays the same. Recompute this number
# whenever the token changes: the exponent is the token's own decimals().
#
# 0.01 ANYONE buys a bundle of 10 credentials. Set a price that is
# commercially real for you before taking payment from anyone.

[[routes]]
prefix = "g.anyone.credentials"
handler_url = "http://claim-minter:8080/"
price = 10000000000000000

# The key document is free: a buyer needs the epoch key before it can blind
# anything, and charging for it makes the protocol undiscoverable.
#
# The handler_url is scoped to /v1/keys/ ON PURPOSE. A request target resolves
# BENEATH the handler path, so pointing this at the issuer root would let a
# caller reach v1/bundles at price = 0 and break the issuer's invariant I6
# ("no free class exists anywhere in this service"). Scoped here, `current` is
# the only reachable target, and `..` is refused before the app is touched.
[[routes]]
prefix = "g.anyone.credentials.keys"
handler_url = "http://issuer:3000/v1/keys/"
price = 0

# /healthz is deliberately NOT routed. The connector's own free, unauthenticated
# /ilp/identity is the public liveness signal; the issuer's health stays inside.

# ============================================================================
# ETHEREUM MAINNET. THIS IS REAL MONEY.
#
# The settlement key at data/evm.key pays real gas and holds this node's real
# earnings. `make keys` generates throwaway key material; do not use it here.
# For development use `make local-e2e`, which settles on a local anvil.
#
# Verified on chain 1 (2026-09-04):
#   registry.getTokenNetwork(ANYONE) -> 0xc24a18f1...4ec60fa8, and ANYONE
#   reports 18 decimals. The connector re-checks both at boot and refuses to
#   start if either has changed.
#
# The public RPC below is fine for a trial and wrong for production, and on an
# onion node it is wrong twice over. It is rate limited and shared -- and it is
# also the one dial this node makes FROM ITS REAL ADDRESS while submitting the
# transactions that redeem its earnings. An observer at that provider sees both
# halves and can link them, which is the linkage an onion endpoint does not
# close (connector ADR 0070 decision 4). Point this at your own node.
# ============================================================================
[settlement.evm]
rpc_url = "https://ethereum-rpc.publicnode.com"
contract_address = "0x61d31e7Fd9a57A0611e29Bd7eB162f15AC8B3427"
token_address = "0xFeAc2Eae96899709a43E252B6B92971D32F9C0F9"
decimals = 18

[settlement.evm.key]
key_file = "/app/data/evm.key"

# Read endpoints need the bearer token; writes need an RFC 9421 signature from
# one of the write keys. redeem-latest lives here, which is how collected claims
# become on-chain money -- without it the node banks nothing. Reachable only on
# the compose network: this surface is never published.
[operator]
bearer_token_file = "/app/data/operator-bearer-token"
write_keys_file = "/app/data/operator-write-keys"
