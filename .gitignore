# Generated key material. Never commit: `make keys` regenerates it.
/data/
node_modules/
.env
/.local-chain/

# The rendered connector config. Generated by scripts/onion-address.sh from
# config/connector.toml plus the address the `anon` daemon generated; it names
# this node's real onion address, and it is regenerated, never edited.
/config/.rendered/

# The hidden-service rehearsal's own channel-nonce watermark. Regenerated.
/.hs-channels.json
/.hs-channels.peers.json
/.local-channels.json
/.local-channels.peers.json
